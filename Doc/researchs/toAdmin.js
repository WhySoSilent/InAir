1. 先在  localhost:8080/admin/QW23ER45T 注册一个模型，成功会得到一个ID
2. 已经自动在 d:/JAVA_DEV/Tomcat7/webapps/ROOT/static/data/models/ 自动创建了一个文件夹，以id命名的
3. 按要求复制文件进去，要求如下

	1.>	js文件以 XXX.js 命名，XXX是id，也是文件夹名称 (模型是二进制版本的JSON文件)
	2.> 手动创建 previews 目录，放一张 preview.png 图片，448 x 280 像素，这是预览图